# Ultra-lightweight requirements for memory-constrained deployments (<500MB)
# Excludes: torch, transformers, sentence-transformers, bertopic, scikit-learn, numpy, textblob
# Makes channels-redis, celery, redis optional (has fallbacks)

Django>=4.2
djangorestframework
channels
beautifulsoup4
lxml
requests
python-dotenv
django-cors-headers
psycopg2-binary
dj-database-url
whitenoise
gunicorn
uvicorn

# Optional dependencies (installed if available, app works without them)
# channels-redis  # Optional - uses in-memory fallback
# celery>=5.0     # Optional - runs synchronously without it
# redis           # Optional - only needed if using Celery/Redis
# kombu>=5.0      # Optional - only needed for Celery
# daphne          # Optional - using gunicorn+uvicorn instead

