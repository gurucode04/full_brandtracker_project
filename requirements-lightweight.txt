# Ultra-lightweight requirements for memory-constrained deployments (<500MB)
# Excludes: torch, transformers, sentence-transformers, bertopic, scikit-learn, numpy, textblob

Django>=4.2
djangorestframework
channels
beautifulsoup4
lxml
requests
python-dotenv
django-cors-headers
psycopg2-binary
dj-database-url
whitenoise
gunicorn
uvicorn
celery>=5.0
kombu>=5.0

# Optional dependencies (installed if available, app works without them)
# channels-redis  # Optional - uses in-memory fallback
# redis           # Optional - only needed if using Celery/Redis (install separately if needed)
# daphne          # Optional - using gunicorn+uvicorn instead

